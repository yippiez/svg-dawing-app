{
  "name": "canvas-drawing-app",
  "version": "1.0.0",
  "description": "A drawing app that uses HTML canvas and outputs in html for embedding in application that use markdown/HTML.",
  "main": "./src/main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --progress --mode=development",
    "start": "webpack serve --progress --mode=development --static=out"
  },
  "keywords": [
    "Drawing",
    "Canvas"
  ],
  "author": "Eren",
  "license": "ISC",
  "devDependencies": {
    "webpack": "^5.90.3",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^5.0.3"
  }
}